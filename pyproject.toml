[project]
name = "alignment-tool"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
    "pandas",
    "numpy<2",
    "networkx",
    "geopy",
    "pyshp",
]
